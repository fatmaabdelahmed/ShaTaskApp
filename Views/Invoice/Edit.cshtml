@using ShaTaskApp.ViewModels.Invoice
@model InvoiceCreateEditViewModel

@{
    ViewData["Title"] = "تعديل الفاتورة #" + Model.ID;
}

@await Html.PartialAsync("_InvoiceForm", Model)

@section Styles {
    <link href="~/css/invoice-form.css" rel="stylesheet" />
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="~/js/invoice-form.js"></script>
}